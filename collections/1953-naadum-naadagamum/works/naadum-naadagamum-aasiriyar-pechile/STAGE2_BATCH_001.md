# Stage 2 — Batch 001 visual text-fidelity audit

Work: **நாடும் நாடகமும் (ஆசிரியர் பேசுகிறார்)**

Controlling source: `TVA_BOK_0064193_நாடும்_நாடகமும்.pdf`

Batch: scans **5–9 / printed 1–5**

## Result

- pages audited directly against source pixels: **5/5**
- Stage 2: **COMPLETE**
- page status after Stage 2: **needs-review 5/5**
- source-proven Stage-2 corrections: **16**
- blocked pages: **0**
- unresolved source-text spans: **1 — scan 9 library/ownership-mark crossing**
- Stage 3 historical-glyph audit: **NOT STARTED**

No OCR, web copy, Wikisource, catalogue text, or alternate edition was used.

## Source-proven corrections

### Scan 5 / printed 1

- `நிலைக் கண்ணாடி` → `நிலைக்கண்ணாடி` in the opening paragraph, matching the joined source form.
- the page-final `தோழனுக்` was rechecked at native resolution; the scan itself visibly ends there. No contextual completion was supplied.

### Scan 6 / printed 2

- `வாழ்வுப் பற்றிய` → `வாழ்வுபற்றிய`
- `எல்லாத் துறைகளிலும்` → `எல்லாத்துறைகளிலும்`
- first `பயன்படுத்துங்கள்.` → `பயன்படுத்துங்கள்,`
- `நல்லவரை எல்லாம்` → `நல்லவரை யெல்லாம்`
- `நாடகக் கலையைப் பயன்படுத்துவது?` → `நாடகக் கலையை பயன்படுத்துவது?`
- `பயன்படுத்துவது, தவறு?` → `பயன்படுத்துவது. தவறு?`
- scan 6→7 physical split of `குறிப்பிட்ட` confirmed as source-visible `குறி` / `பிட்ட`; preserved.

### Scan 7 / printed 3

- punctuation after `காவியமானாலும்`: comma → period
- punctuation after `சிற்பக் கலையானாலும்`: comma → period
- `இதனைய` → `இத்தகைய`
- the source reading `தீயீடு` was rechecked and retained for the Stage-3 glyph audit.

### Scan 8 / printed 4

- `சைவம் பற்றிய நினைப்பையும்` → `சைவம் பற்றி நினைப்பையும்`
- `நமது கண் முன்னே` → `நமது கண்ணெதிரே`
- Krishna/Gopika paragraph punctuation and the visible `கண்ணனே கரங் கூப்பி` span were rechecked directly.

### Scan 9 / printed 5

- `நரசிம்ம அவதாரம்` → `நரசிம்மாவதாரம்`
- `பரப்புகிறது!` → `பரப்புகிறதே!`
- `அவசியடித்துக்` → `அலறியடித்துக்`
- `அளவந்தாரையும்` → source-visible `ஆளவந்தாரையும்`

## Remaining review item

On scan **9**, a library/ownership mark crosses the printed line immediately after:

`‘மத நம்பிக்கை’ என்ற போர்வை`

Native pixels, enlarged crops, contrast variants, nearest-neighbour review, and RGB/channel-separation variants were inspected. The words covered by the mark are still not defensibly recoverable, so **no completion is guessed**.

The source-visible right side `தந்திரம் ஆளவந்தாரையும் கேட்டிறேன்` is retained. The obscured words immediately before `தந்திரம்` remain in the review queue for later exhaustive/final checking.

## Exact next activity

Run **Stage 3 historical Tamil glyph audit** on the same scans **5–9 / printed 1–5**.

Explicitly audit historical identities including `ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ` without global replacement or modernization.

Keep all pages `needs-review`; do not start Stage 4 or scans 10–14 in the same activity.
