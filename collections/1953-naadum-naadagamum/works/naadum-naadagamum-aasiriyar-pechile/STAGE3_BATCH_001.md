# Stage 3 — Batch 001 historical Tamil glyph audit

Work: **நாடும் நாடகமும் (ஆசிரியர் பேசுகிறார்)**

Controlling source: `TVA_BOK_0064193_நாடும்_நாடகமும்.pdf`

Batch: scans **5–9 / printed 1–5**

## Result

- pages independently reopened from source pixels: **5/5**
- mandatory historical-glyph families explicitly audited: **13/13**
- Stage 3: **COMPLETE**
- character-identity corrections: **0**
- unresolved historical-glyph clusters: **0**
- page status after Stage 3: **needs-review 5/5**
- remaining non-glyph source-text issue: **1 — scan 9 library/ownership-mark crossing**
- Stage 4: **NOT STARTED**

No OCR, web copy, Wikisource, catalogue text, alternate edition, global replacement, spelling modernization, or grammar normalization was used.

## Mandatory-family coverage

The full pages were checked for:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Families visibly represented in this batch were rechecked at whole-word / phrase level. No source-supported character-identity change was required.

Families for which no source candidate was found in this batch after full-page review: **`ணை / ணொ / ணோ / றொ / னொ / னோ`**.

## Representative source checks

- scan 5: `கண்ணாடி`, `நிலைக்கண்ணாடி` (`ணா`); `கலை` (`லை`); `காட்சிகளைப்` (`ளை`); `பேசுகிறார்` (`றா`); `போதனைக்கு` (`னை`)
- scan 6: `கலையை`, `கலையானால்` (`லை`); `நாடகங்களைப்` (`ளை`); `கலையானால்` (`னா`)
- scan 7: `காவியமானாலும்`, `கலையானாலும்` (`னா`); `தலைவன்`, `தலைவி` (`லை`); `கூறாமலிருக்கின்றதா` (`றா`); `காண்கிறோம்` (`றோ`)
- scan 8: `வேலைகளைப்பற்றிய`, `சேலைகளைத்` (`லை/ளை`); `வேலைப்பாட்டையுமேதானா` (`னா`); `காண்கிறோம்` (`றோ`); `நினைவிற்கு` (`னை`)
- scan 9: `சேலைகளின்`, `கலை` (`லை`); `மரக்கிளையிலே` (`ளை`); `இரணியனைப்`, `நினைவிற்கு` (`னை`)

The Stage-2 reading `தீயீடு` was visually rechecked but is not one of the 13 reform-sensitive families; it remains unchanged.

## Remaining source-text issue

The library/ownership mark on scan 9 still crosses part of the printed line after:

`‘மத நம்பிக்கை’ என்ற போர்வை`

This is a **source obstruction**, not an unresolved historical-glyph identity. Stage 3 does not manufacture a reading.

## Exact next activity

Run **Stage 4 final independent source check** on scans **5–9 / printed 1–5**.

Reopen all five pages fresh and verify the complete Stage-3 committed text against the scan pixels. Apply the exhaustive difficult-reading protocol to the scan-9 mark-crossed span. Promote pages to `verified` only if every source-text issue receives a defensible final disposition.

Do not begin scans 10–14 in the same activity.
