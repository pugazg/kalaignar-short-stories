# Stage 3 — Batch 4 historical Tamil glyph audit

Work: **நாடும் நாடகமும் (ஆசிரியர் பேசுகிறார்)**  
Controlling source: `TVA_BOK_0064193_நாடும்_நாடகமும்.pdf`  
Batch: scans **20–24 / printed 16, 16-A–16-D**

## Result

- pages reopened for glyph-focused direct-scan review: **5/5**
- mandatory historical-glyph families audited: **13/13**
- visibly represented families: **8/13**
- absent families: **5/13**
- character-identity corrections: **0**
- unresolved glyph clusters: **0**
- Stage-2 ordinary-text corrections preserved: **12**
- unresolved ordinary source-text spans: **0**
- guessed readings: **0**
- page status after Stage 3: **needs-review 5/5**
- Stage 4 final independent source check: **NEXT**

No OCR, web copy, Wikisource, catalogue text, alternate edition, or contextual reconstruction was used as source authority.

## Mandatory-family disposition

### Present and source-confirmed

- `ணா`
  - scan 20: `கண்ணாடிகளே`, `கண்ணாடிகளை`
  - scan 22: `அண்ணாவும்`, `அண்ணாதுரை`
- `ணை`
  - scan 21: `துணைக்கொண்டு`, `துணையெனக்`
  - scan 23: `துணை`
  - scan 24: `துணையாகத்தான்`, `துணையாக`
- `லை`
  - scan 20: `நிலைக்`, `நிலைமையைக்`
  - scan 21: `கலையார்வத்துடனும்`, `கலையில்`
  - scan 23: `கலைக்கும்`, `கலைக்கு`
  - scan 24: `கலைக்குக்`, `கலையென்றுதான்`
- `ளை`
  - scan 20: `தொல்லைகளைத்`
  - scan 21: `நாடகங்களைக்`, `நாடகங்களை`
  - scan 22: `கருத்துக்களை`
  - scan 23: `பாத்திரங்களையும்`
  - scan 24: `மக்களை`

> **Stage-4 supersession note:** the Stage-3 page review had retained `ஆடைகளை` as an ordinary-text reading. The final independent Stage-4 source check resolves the physical line split as `ஆடை` / `கள்`, yielding `ஆடைகள்`, so that word is no longer a `ளை` example. The family-level Stage-3 result remains unchanged because source-visible `மக்களை` independently confirms `ளை` on scan 24.
- `றா`
  - scan 20: `என்றாவது`
  - scan 22: `நடிக்கிறார்`
  - scan 23: `வருகிறார்`
  - scan 24: `பேசுகிறார்களே`
- `றோ`
  - scan 23: `அணிகிறோம்`
  - scan 24: `யணிகிறோம்`, `வருகிறோம்`, `படுத்துகிறோம்`
- `னா`
  - scan 20: `ஆனால்`
  - scan 21: `ஆனால்`
  - scan 22: `அதனால்`
  - scan 23: `கூறினார்`, `நடித்துக்காட்டினார்`, `அதனால்`
- `னை`
  - scan 20: `இதனை`, `இதனையே`
  - scan 21: `வேதனைச்`
  - scan 22: `பலனையும்`
  - scan 24: `ஆண்டவனைக்`

### Absent from Batch 4 text

- `ணொ`
- `ணோ`
- `றொ`
- `னொ`
- `னோ`

The absent families were explicitly checked against the full committed Batch-4 text and source pages; no candidate requiring character-identity adjudication was found.

## Page disposition

### Scan 20 / printed 16

Representative source clusters checked at enlarged/native resolution include `கண்ணாடிகளே` / `கண்ணாடிகளை` (`ணா`), `நிலைக்` / `நிலைமையைக்` (`லை`), `தொல்லைகளைத்` (`ளை`), `என்றாவது` (`றா`), `ஆனால்` (`னா`), and `இதனை` / `இதனையே` (`னை`).

Character-identity corrections: **0**.

### Scan 21 / printed 16-A

Representative source clusters checked include `துணைக்கொண்டு` / `துணையெனக்` (`ணை`), `கலையார்வத்துடனும்` / `கலையில்` (`லை`), `நாடகங்களைக்` / `நாடகங்களை` (`ளை`), `ஆனால்` (`னா`), and `வேதனைச்` (`னை`).

Character-identity corrections: **0**.

### Scan 22 / printed 16-B

Representative source clusters checked include `அண்ணாவும்` / `அண்ணாதுரை` (`ணா`), `நாடகக்கலையை` (`லை`), `கருத்துக்களை` (`ளை`), `நடிக்கிறார்` (`றா`), `அதனால்` (`னா`), and `பலனையும்` (`னை`).

Character-identity corrections: **0**.

### Scan 23 / printed 16-C

Representative source clusters checked include `துணை` (`ணை`), `கலைக்கும்` / `கலைக்கு` (`லை`), `பாத்திரங்களையும்` (`ளை`), `வருகிறார்` (`றா`), `அணிகிறோம்` (`றோ`), and `கூறினார்` / `நடித்துக்காட்டினார்` (`னா`).

Character-identity corrections: **0**.

### Scan 24 / printed 16-D

Representative source clusters checked include `துணையாகத்தான்` / `துணையாக` (`ணை`), `கலைக்குக்` / `கலையென்றுதான்` (`லை`), `மக்களை` (`ளை`), `பேசுகிறார்களே` (`றா`), `யணிகிறோம்` / `வருகிறோம்` / `படுத்துகிறோம்` (`றோ`), and `ஆண்டவனைக்` (`னை`).

Stage 4 later supersedes the ordinary-text reading `ஆடைகளை` with source `ஆடைகள்`; this does not change the Stage-3 character-identity result.

Character-identity corrections: **0**.

## Boundary preservation

Stage 3 does not alter the Stage-2 boundary findings:

- scan 19→20 — no split
- scan 20→21 — no split
- scan 21→22 — `கொண்டிட` / `வேண்டாம்.`
- scan 22→23 — `நல்ல` / `சமூக நாடகங்கள்`
- scan 23→24 — no split
- scan 24 — prose/discourse work-ending
- scan 25 — separate `தெருக்கூத்து` opening; excluded

## Status

- Stage 1: **COMPLETE 5/5**
- Stage 2: **COMPLETE 5/5**
- Stage-2 corrections: **12**
- Stage 3: **COMPLETE / PASS 5/5**
- mandatory glyph families audited: **13/13**
- present families: **8/13**
- absent families: **5/13**
- character-identity corrections: **0**
- unresolved glyph clusters: **0**
- unresolved ordinary source-text spans: **0**
- guessed readings: **0**
- pages: **needs-review 5/5**
- Stage 4: **NEXT**

## Exact next activity

Run **Batch 4 Stage 4 final independent source check** for scans **20–24 / printed 16, 16-A–16-D**.

Reopen all five pages fresh against the controlling source, verify all 12 Stage-2 corrections and all Stage-3 glyph dispositions, reconfirm every physical boundary and the scan-24 work ending, promote pages to `verified` only if no issue remains, synchronize controls, commit Stage 4, and stop before `தெருக்கூத்து`.
